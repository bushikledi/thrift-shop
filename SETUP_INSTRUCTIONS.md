# Setup Instructions

This document has been superseded by the [README](./README.md), which now
covers setup, configuration, seeded test accounts and troubleshooting in one
place.

It previously described a set of pages as newly created and several features
(analytics charts, payment methods) as placeholders. That is no longer
accurate, and the environment setup it described had drifted from
`docker-compose.yml` — so it is kept only as a redirect rather than a second,
conflicting source of truth. Safe to delete.

- **Getting the stack running:** [README → Quick Start](./README.md#quick-start)
- **Logins for every role:** [README → Test Accounts](./README.md#test-accounts)
- **Configuration:** [README → Configuration](./README.md#configuration)
- **Things that commonly go wrong:** [README → Troubleshooting](./README.md#troubleshooting)
- **Load testing:** [`load-tests/README.md`](./load-tests/README.md)
