# ================================
# Docker Compose Environment Variables
# ================================
# Copy this file to .env and adjust values as needed

# Node Environment
NODE_ENV=development

# ================================
# Database (PostgreSQL)
# ================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=thriftshop
POSTGRES_PORT=5432

# ================================
# Redis
# ================================
REDIS_PORT=6379

# ================================
# MinIO (S3-compatible storage)
# ================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# ================================
# Email (MailHog/Mailpit)
# ================================
MAILHOG_SMTP_PORT=1025
MAILHOG_WEB_PORT=8025

# ================================
# API Configuration
# ================================
API_PORT=3000
JWT_SECRET=your-super-secret-jwt-key-change-in-production-minimum-64-characters-long
JWT_EXPIRES_IN=7d
CORS_ORIGINS=http://localhost:3001

# Encryption key for sensitive stored data (vendor payout/bank details).
# Optional: derived from JWT_SECRET when unset. Generate: openssl rand -hex 32
# ENCRYPTION_KEY=

# ================================
# App Configuration
# ================================
APP_PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:3000

# ================================
# Stripe (optional - card payments)
# ================================
# Leave unset to run cash-on-delivery only.
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_CURRENCY=usd
